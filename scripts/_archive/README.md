# _archive — deprecated

- `matte_video_isnet.py` — legacy auto-extract version of `matte.py` (handled ffmpeg internally). Replaced by `../service-animation/matte.py` (clean batch `frames_in → frames_out` + separate `process_service.sh`). Kept for history, not used in build. Remove if not needed.
